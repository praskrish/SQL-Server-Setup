---
- hosts: all
  tasks:
    - name: stop ms sql server
      systemd:
        name: mssql-server
        state: stopped
        enabled: no
    
    - name: Ansible delete msql files
      shell: rm -rf /var/opt/mssql/

